@model IEnumerable<haver.Models.SalesOrder>

<partial name="_ViewHeading" />

<h1 class="text-center my-4">@ViewData["ControllerFriendlyName"]</h1>

<div class="d-flex justify-content-between align-items-center mb-3">
    <a asp-action="Create" class="btn btn-primary">
        <i class="fa-solid fa-plus"></i> Create New
    </a>
</div>

<form asp-action="Index" method="get">
    <input type="hidden" name="sortDirection" value="@ViewData["sortDirection"]" />
    <input type="hidden" name="sortField" value="@ViewData["sortField"]" />

    <div class="form-horizontal">
        <button class="btn @ViewData["Filtering"] mb-3" type="button" data-bs-toggle="collapse" id="filterToggle" data-bs-target="#collapseFilter" aria-expanded="false" aria-controls="collapseFilter">
            <i class="fa-solid fa-filter"></i> Filter/Search @ViewData["numberFilters"]
        </button>
        <div class="collapse @ViewData["ShowFilter"]" id="collapseFilter">
            <div class="card card-body bg-light">
                <div class="row">
                    <div class="form-group col-md-4">
                        <label class="form-label fw-bold">Search Order Number:</label>
                        @Html.TextBox("SearchOrder", null, new { @class = "form-control", placeholder = "Enter Order Number" })
                    </div>
                    <div class="form-group col-md-4">
                        <label class="form-label fw-bold">Search PO Number:</label>
                        @Html.TextBox("SearchPO", null, new { @class = "form-control", placeholder = "Enter PO Number" })
                    </div>
                    <div class="form-group col-md-4 align-self-end">
                        <input type="submit" name="actionButton" value="Filter" class="btn btn-outline-primary" />
                        <a asp-action="Index" class="btn btn-outline-secondary">Clear</a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <table class="table table-striped table-bordered table-hover shadow-sm">
        <thead class="bg-dark text-white text-center">
            <tr>
                <th>
                    <button class="btn btn-link bb text-white p-0 fw-bold text-decoration-none" type="submit" name="actionButton" value="OrderNumber">
                        Order Number
                        @if (ViewData["sortField"]?.ToString() == "OrderNumber")
                        {
                            <i class="fa-solid @(ViewData["sortDirection"]?.ToString() == "asc" ? "fa-arrow-up" : "fa-arrow-down")"></i>
                        }
                    </button>
                </th>
                <th>
                    <button class="btn btn-link bb text-white p-0 fw-bold text-decoration-none" type="submit" name="actionButton" value="SoDate">
                        SO Date
                        @if (ViewData["sortField"]?.ToString() == "SoDate")
                        {
                            <i class="fa-solid @(ViewData["sortDirection"]?.ToString() == "asc" ? "fa-arrow-up" : "fa-arrow-down")"></i>
                        }
                    </button>
                </th>
                <th>
                    <button class="btn btn-link bb  text-white p-0 fw-bold text-decoration-none" type="submit" name="actionButton" value="PoNumber">
                        PO Number
                        @if (ViewData["sortField"]?.ToString() == "PoNumber")
                        {
                            <i class="fa-solid @(ViewData["sortDirection"]?.ToString() == "asc" ? "fa-arrow-up" : "fa-arrow-down")"></i>
                        }
                    </button>
                </th>
                <th>
                    <button class="btn btn-link text-white bb  p-0 fw-bold text-decoration-none" type="submit" name="actionButton" value="MachineSchedule">
                        Machine Schedule
                        @if (ViewData["sortField"]?.ToString() == "MachineSchedule")
                        {
                            <i class="fa-solid @(ViewData["sortDirection"]?.ToString() == "asc" ? "fa-arrow-up" : "fa-arrow-down")"></i>
                        }
                    </button>
                </th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.OrderNumber)</td>
                    <td>@Html.DisplayFor(modelItem => item.SoDate)</td>
                    <td>@Html.DisplayFor(modelItem => item.PoNumber)</td>
                    <td>@Html.DisplayFor(modelItem => item.MachineSchedule.Summary)</td>
                    <td class="text-center">
                        <a asp-action="Edit" asp-route-id="@item.ID" class="btn btn-sm mx-1">
                            <i class="fa-solid fa-pen-to-square"></i> Edit
                        </a>
                        <a asp-action="Details" asp-route-id="@item.ID" class="btn btn-sm mx-1 text-white">
                            <i class="fa-solid fa-circle-info"></i> Details
                        </a>
                        <a asp-action="Delete" asp-route-id="@item.ID" class="btn btn-sm mx-1">
                            <i class="fa-solid fa-trash"></i> Delete
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <partial name="_PagingNavBar" />
</form>

@section Scripts {
    <script type="text/javascript">
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl)
        })
    </script>
}

<style>
    .bb {
        background: none;
        outline: none;
    }
</style>
